{
  "schemaVersion": 1,
  "id": "io.github.dicemans.docker-vms",
  "name": "Docker VMs",
  "version": "1.2.0",
  "author": "Paolo Pellicori",
  "license": "MIT",
  "description": "Start, restart, stop, pause and remove Docker containers from the bar, read their logs, open a shell, and open a desktop session on Windows VMs. When docker itself is out of reach — daemon down, or no access to the socket — one button fixes it behind a single polkit authorization.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "Docker VMs",
    "description": "Container control with logs, shell and an opt-in performance monitor, plus an RDP session and web viewer for dockur Windows/macOS VMs.",
    "category": "System",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "refreshIntervalSec": 5,
      "nameFilter": "",
      "vmsOnly": false,
      "showCount": true,
      "stopTimeoutSec": 60,
      "rdpTimeoutSec": 120,
      "showStats": false,
      "statsIntervalSec": 15
    },
    "schema": [
      {
        "key": "refreshIntervalSec",
        "type": "integer",
        "label": "Refresh interval (seconds)",
        "min": 2,
        "max": 300,
        "step": 1,
        "defaultValue": 5,
        "description": "How often the container list is refreshed while the panel is open. Closed, it refreshes every 30 seconds at most."
      },
      {
        "key": "stopTimeoutSec",
        "type": "integer",
        "label": "Graceful stop timeout (seconds)",
        "min": 1,
        "max": 600,
        "step": 5,
        "defaultValue": 60,
        "description": "How long docker may spend on a clean shutdown before it kills the container. Docker's own default without this is 10 seconds, which is a power cut for a virtual machine."
      },
      {
        "key": "rdpTimeoutSec",
        "type": "integer",
        "label": "RDP connect timeout (seconds)",
        "min": 5,
        "max": 600,
        "step": 10,
        "defaultValue": 120,
        "description": "How long to wait for a cold VM to answer on RDP before giving up."
      },
      {
        "key": "showStats",
        "type": "boolean",
        "label": "Show CPU and memory",
        "defaultValue": false,
        "description": "Adds a performance line to every running container. Off by default: the docker call behind it waits about two seconds each time, against roughly sixty milliseconds for the whole listing. It runs on its own slower timer so the list stays responsive."
      },
      {
        "key": "statsIntervalSec",
        "type": "integer",
        "label": "Performance refresh (seconds)",
        "min": 5,
        "max": 300,
        "step": 5,
        "defaultValue": 15,
        "description": "How often CPU and memory are sampled while the performance monitor is on."
      },
      {
        "key": "nameFilter",
        "type": "string",
        "label": "Name filter",
        "defaultValue": "",
        "description": "Only show containers whose name contains this text. Empty shows all of them."
      },
      {
        "key": "vmsOnly",
        "type": "boolean",
        "label": "Only show VMs",
        "defaultValue": false,
        "description": "Hide plain containers and list only Windows and macOS VMs."
      },
      {
        "key": "showCount",
        "type": "boolean",
        "label": "Show running count in the bar",
        "defaultValue": true
      }
    ]
  }
}
